services:
  - type: web
    name: multi-agent-tourism
    env: python
    rootDir: .
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
    autoDeploy: true
    healthCheckPath: "/"
